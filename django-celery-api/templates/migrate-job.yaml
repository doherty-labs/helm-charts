apiVersion: batch/v1
kind: Job
metadata:
  name: django-migrate
spec:
  template:
    spec:
      containers:
        - name: django-migrate-container
          image: "{{ .Values.service.image.repository }}:{{ .Values.service.image.tag }}"
          command: ["python", "manage.py", "migrate"]
          {{- with .Values.service.resources }}
            resources:
              {{- toYaml . | nindent 6 }}
          {{- end }}
          workingDir: /usr/src/app/django
          env:
            {{- include ".Values.jobs.migrate.env" . | nindent 6 }}
      restartPolicy: Never
  backoffLimit: 1